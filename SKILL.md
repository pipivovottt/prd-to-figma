---
name: prd-to-figma
description: 将产品需求文档（PRD）分析为结构化页面描述，经用户确认后调用项目设计系统在 Figma 中逐页生成设计稿。用户提供 PRD 后自动运行整条流水线；支持多项目路由（当前支持 FinEAM），可通过 references/projects-info.md 扩展新项目。
---

# PRD → Figma 设计稿生成流水线

---

## Phase 1 — PRD 分析

### 步骤 0：项目识别

读取 `references/projects-info.md`，从 PRD 标题、产品名、系统名、业务词中匹配「识别关键词」。

- **匹配成功**：加载该项目的「页面框架」「组件约束」「页面模版」，作为后续步骤 2、5 的上下文
- **未匹配**：继续执行，步骤 2 和 5 按通用规则处理；输出末尾注明「未匹配到项目约定，建议补充 projects-info.md」

### 步骤 1：页面 / 路由拆分

将 PRD 功能点映射到具体页面，列出页面清单（路由路径 + 核心功能意图）。

判断准则：
- 弹窗、抽屉、Tab 内容 → 属于同一页
- 需要跳转的独立视图 → 独立页面
- 全局组件（导航栏、通知、权限拦截）→ 单独标注，不计入页面数量

### 步骤 2：信息架构 & 导航结构

画出页面层级树，确认导航入口和路径，不用深入描述每个导航组件的字段。

### 步骤 3：用户任务流（只标注关键操作）

对每个有交互行为的操作节点，标注：
- 触发方式（点击 / 提交 / 滚动…）
- 目标（跳转页面 / 打开弹窗 / 触发接口…）
- 成功 / 失败后的反馈与流向

### 步骤 4：数据模型 → 字段映射

将 PRD 中的业务字段映射到页面展示字段，定义字段规范：

| 规范维度 | 示例 |
|----------|------|
| 数值精度 | 千分位，保留 2 位小数 |
| 百分比 | 带正负号，保留 2 位小数 |
| 日期时间 | `YYYY/MM/DD HH:mm:ss` |
| 枚举值 | 列出可选项，如 USD / HKD / CNY |
| 颜色语义 | 正值红色 / 负值绿色 / 零值默认色 |
| 空值处理 | 显示 `--` 或空态占位组件 |

### 步骤 5：组件选型

为每种 UI 需求指定组件类型。优先使用通用组件名称（与设计系统无关）：

Tag / Badge / Button / TextButton / Input / Select / Table / DataTable / Card / Modal / Drawer / Tabs / SegmentedControl / Pagination / Tooltip / Toast / Empty / Skeleton…

### 步骤 6：状态矩阵枚举

对每个页面列出需要覆盖的状态：

- **页面级**：加载中 / 加载失败 / 无数据 / 无权限
- **列表 / 表格**：空列表 / 分页加载中 / 加载更多失败
- **表单字段**：默认 / 聚焦 / 填写中 / 校验失败 / 禁用
- **按钮**：默认 / hover / loading / disabled
- **数值**：正值 / 负值 / 零值 / 缺省（--）

---

## Phase 1 输出规则

完成步骤 0–6 后，按页面输出，**每个页面一个独立代码块**：

~~~
## 页面：[页面名称]（[路由路径]）

**布局**：→ 基于模版 `[模版名称]`          ← 命中项目页面模版时用此写法
         或 [整体布局结构，一句话]           ← 无匹配模版时用此写法

### 一、[区块名称]
[区块描述]

### 二、[区块名称]
…
~~~

**简化规则（必须遵守）**：

1. 固定 / 通用组件 → 一句话带过，不逐字描述内部字段
2. 字段值 → 用规范替代具体数值（`千分位，保留 2 位小数` 而非 `999,999.00`）
3. 每个组件描述三要素：类型 + 状态 + 字段规范
4. 表格列用表格描述，操作列单独列举按钮
5. 弹窗 / 抽屉作为所属页面的子区块描述，不单独成页
6. 命中项目页面模版 → 布局字段替换为模版引用，不再写布局文字描述

---

## 检查点

输出所有页面描述后，向用户发送：

> 以上共 **N 个页面**描述已生成完毕。
>
> 如需继续生成 Figma 设计稿，请：
> 1. 确认以上内容（如需修改，请直接说明）
> 2. 提供目标 Figma 文件链接（格式：`https://www.figma.com/design/XXXX/...`）
>
> 也可指定只生成其中某几个页面（如「只生成第 1、3 页」）。

**等待用户回复后再执行 Phase 2。**

---

## Phase 2 — Figma 页面生成

### 准备工作

1. 从用户提供的 Figma URL 解析 `fileKey`（`figma.com/design/:fileKey/...`）
2. 重新读取 `references/projects-info.md`，获取当前项目的「设计参考文件」路径
3. 读取该设计参考文件（含组件 key 速查表 + 项目专属规范）
4. 读取 `/Users/xindih/.cursor/plugins/cache/cursor-public/figma/3590366424deba5651026319b71b291d10004f1b/skills/figma-use/SKILL.md`（**必须在任何 `use_figma` 调用前完成**）

### 逐页绘制

对每个确认的页面描述，按顺序执行：

**① 创建页面 Frame**
- 在目标 Figma 文件中创建 1920×1080 Frame
- 命名格式：`页面名称（路由路径）`
- 多个 Frame 水平排列，间距 80px

**② 按设计参考文件的工作流拼装组件**
- 遵循设计参考文件中的：页面模版识别 → 组件导入 → 变体选择 → 示例数据填充 → 项目专属规范
- 不得手动绘制可从组件库找到的元素

**③ 完成后打印进度**
- 格式：`✓ [N/总数] 页面名称（路由路径）`

**④ 设计稿自检（所有页面生成完毕后执行一次）**

对本次生成的所有页面及弹窗，逐条执行以下断言。发现不符项，立即通过 `use_figma` 修正后再继续。

---

**【CHECK-01】弹窗 / 抽屉 body 帧无意外纵向 padding**

**适用场景**：页面中存在通过 `detachInstance()` 重建了 body 内容区的 modal / modal-confirm / Drawer。

**检查方式**：
```javascript
// 对每一个 detach 后的弹窗 frame 执行
const body = modalFrame.findOne(n => n.name === 'body');
console.log('paddingTop:', body?.paddingTop, 'paddingBottom:', body?.paddingBottom);
// 期望值：均为 0
```

**修正方式**：若 `paddingTop` 或 `paddingBottom` 非零且无主动设计意图（即是重建时随手填写的经验值），立即归零：
```javascript
body.paddingTop = 0;
body.paddingBottom = 0;
```

**根因说明**：modal 的 `body` 容器帧在原组件中纵向 padding 为 0，上下留白由外层 modal frame 的 `itemSpacing` 控制。若在重建时额外添加了 `paddingTop/Bottom`，会与 `itemSpacing` 叠加，产生过大的空白间距。

---

所有页面绘制完毕且自检通过后，汇报：完成数量、Figma 文件链接、任何失败项及原因。
